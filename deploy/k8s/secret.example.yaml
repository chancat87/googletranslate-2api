apiVersion: v1
kind: Secret
metadata:
  name: googletranslate-2api-secret
  labels:
    app.kubernetes.io/name: googletranslate-2api
type: Opaque
# 示例文件不包含真实 data。
# 生产用 kubectl create secret generic googletranslate-2api-secret \
#   --from-literal=GOOGLE_API_KEY=<real-key> \
#   --from-literal=API_MASTER_KEY=<strong-random-key>
